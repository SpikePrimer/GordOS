/**
 * Set your Cycle Login API URL so accounts work across devices.
 * Run the server: cd cycle-login/server && npm install && npm start
 * Then set the URL below to your server (e.g. https://your-server.com or http://localhost:3847).
 * Leave empty or comment out for local-only (accounts only on this device).
 */
window.CYCLE_LOGIN_API_URL = '';  // e.g. 'https://your-domain.com' or 'http://localhost:3847'
